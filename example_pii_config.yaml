# pg-snap-anon PII Configuration
# Generated by: pg-snap-anon --configure
#
# This file declares which tables and columns contain PII and what
# Faker rule to apply. Only columns listed here will be anonymized.
# Everything else is left untouched.
#
# approved: must be set to true before --run will execute
# approved_by: name of DBA who reviewed and approved this config
# approved_date: date of approval (YYYY-MM-DD)

tables:
  users:
    email: fake.email()
    full_name: fake.name()
    phone_number: fake.phone_number()
    username: fake.user_name()
    job_title: fake.job()
  organizations:
    company_name: fake.company()
    billing_address_street: fake.street_address()
    billing_address_city: fake.city()
    billing_address_state: fake.state()
    billing_address_zip: fake.postcode()
  payment_methods:
    billing_name: fake.name()

approved: false
approved_by: ""
approved_date: ""
